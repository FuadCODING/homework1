const money = prompt("введите сумму в долларах");
const currency = prompt("введите в какую сумму хотите перевести");
if (currency === 'azn') {alert(money*1.7)}
if (currency === 'uan') {alert(money*7,14)}
if (currency === 'eur') {alert(money*0.9)}
